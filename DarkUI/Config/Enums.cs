namespace DarkUI
{
    public enum DarkButtonStyle
    {
        Normal,
        Flat
    }

    public enum DarkControlState
    {
        Normal,
        Hover,
        Pressed
    }

    public enum DarkContentAlignment
    {
        Center,
        Left,
        Right
    }
}
